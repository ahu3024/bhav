{
  "name": "bhav-whatsapp-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "open-wa bridge \u2014 gives the Python backend one HTTP endpoint to send WhatsApp alerts from a real WhatsApp account.",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "link": "node --env-file-if-exists=.env server.js --show-qr"
  },
  "dependencies": {
    "@open-wa/wa-automate": "^4.76.0",
    "express": "^4.21.2",
    "qrcode-terminal": "^0.12.0"
  }
}
