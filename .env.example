# Frontend build settings.
#
# Vite inlines these at BUILD time, so changing one needs a rebuild, not a
# restart. Copy to .env.local for local development.

# The API's full URL, scheme included. Defaults to http://localhost:8000.
#   VITE_API_URL=https://bhav-api.onrender.com
VITE_API_URL=
